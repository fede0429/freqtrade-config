# Commit Suggestion

```bash
git add agent_service/providers agent_service/aggregator docs/agent_integration
git commit -m "Integrate decision aggregator into decision cache generation chain"
git push
```
