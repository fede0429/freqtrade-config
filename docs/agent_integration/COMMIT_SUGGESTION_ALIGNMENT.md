# Commit Suggestion

```bash
git add user_data/strategies user_data/config agent_service/apps docs/agent_integration scripts
git commit -m "Align agent bridge callbacks, config, traces, and replay compare"
git push
```
