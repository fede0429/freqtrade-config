# Commit Suggestion

```bash
git add agent_service user_data/config user_data/strategies scripts docs/agent_integration docs/news_signal_layer .env.example .env.real_integration.example
git commit -m "Add current full snapshot v3"
git push
```
