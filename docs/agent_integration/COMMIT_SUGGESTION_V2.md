# Commit Suggestion V2

建议 commit message:

```bash
git add user_data/strategies user_data/config docs/agent_integration
git commit -m "Add agent bridge v2 with shadow audit and stake-only rollout"
```
