version: '3.9'
services:
  reporter-daily:
    image: python:3.11-slim
    working_dir: /workspace
    volumes:
      - ./:/workspace
    command: >
      bash -lc "PYTHONPATH=. python3 apps/reporter/performance_report.py --profile config/reporting/runtime/spot.paper.json"
